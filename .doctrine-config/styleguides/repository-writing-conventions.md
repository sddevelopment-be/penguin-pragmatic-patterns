# Repository Writing Conventions (Local)

Source: migrated from legacy `.github/agents` conventions on 2026-02-12.

For this repository:

- Primary writing rules live in `docs/styleguide/`.
- Prefer existing tone, structure, and terminology used in current pattern/primer content.
- Use `docs/templates/` when creating new long-form docs.

This local file exists to bridge upstream doctrine references that may point to `docs/styleguides/` in framework repositories.
