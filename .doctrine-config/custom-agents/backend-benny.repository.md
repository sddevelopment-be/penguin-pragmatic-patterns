# Backend Benny Repository Notes

Repository-local adaptation for `backend-benny`:

- In this repository, backend-oriented tasks often involve Hugo build/config integration (`config.yaml`, `assets/`, build pipelines) rather than service APIs.
- Use `.doctrine-config/directives/003_repository_quick_reference.md` before making structural changes.
- Respect content/style constraints in `docs/styleguide/` when generating documentation artifacts.
