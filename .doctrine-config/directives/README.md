# Local Directives

Repository-local directive overrides and supplements.

Rules:

- These directives are additive to the upstream doctrine stack.
- They must not override General or Operational guidelines from `.github/agents/guidelines/`.
- Use these files for repository-specific constraints only.

Current local directives:

- `003_repository_quick_reference.md` - Hugo-specific repository topology and runtime notes.
